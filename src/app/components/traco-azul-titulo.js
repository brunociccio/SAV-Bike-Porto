"use client"
import styled from 'styled-components';

const TracoAzul = styled.img`
    max-width: 50%;
    height: auto;
`;

export function TracoAzulTitulo({ src, alt }) {
    return <TracoAzulTitulo src={src} alt={alt} />;
}
